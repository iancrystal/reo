source :gemcutter

gem 'geokit', '1.5.0'
gem "rails", "2.3.2"
#gem "pg", :group => :production
gem 'mysql', :group => :development 
gem 'ym4r'
gem 'aws-s3'
gem 'ruby-debug', :group => :development

# since we cannot have different versions of the same gem even with the :group constraint, just comment/uncomment depending on the platform

#rmagick 2.12.2 is required in heroku because it is linux
gem 'rmagick', '2.12.2', :group => :production

# rmagick 2.12.0 is required in windows
#gem 'rmagick', '2.12.0'

# important: make sure to remove Gemfile.lock before running git commit because rmagick settings for window and linux are not compatible

